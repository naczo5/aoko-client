<Window x:Class="Aoko.WindowPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Java client"
        Width="620" Height="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource BgBrush}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Select Java client"
                   Foreground="{DynamicResource TextBrush}"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,6"/>

        <TextBlock Grid.Row="1"
                   Text="Multiple Java/Minecraft processes were found. Select the client process to inject into."
                   Foreground="{DynamicResource DimTextBrush}"
                   FontSize="11"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>

        <Border Grid.Row="2"
                Background="{DynamicResource PanelBrush}"
                BorderBrush="{DynamicResource SliderBgBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="4">
            <ListBox x:Name="WindowList"
                     Background="Transparent"
                     BorderThickness="0"
                     Foreground="{DynamicResource TextBrush}"
                     SelectionMode="Single">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding DisplayLabel}"
                                   Foreground="{DynamicResource TextBrush}"
                                   TextWrapping="Wrap"
                                   Margin="4,4"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <Button Grid.Row="3"
                x:Name="RefreshButton"
                Content="Refresh list"
                Style="{StaticResource DarkButton}"
                HorizontalAlignment="Left"
                Margin="0,10,0,0"
                Padding="12,6"
                Click="RefreshButton_Click"/>

        <DockPanel Grid.Row="4" Margin="0,12,0,0">
            <TextBlock Text="Bridge version"
                       Foreground="{DynamicResource TextBrush}"
                       FontSize="12"
                       VerticalAlignment="Center"
                       DockPanel.Dock="Left"
                       Margin="0,0,10,0"/>
            <ComboBox x:Name="VersionCombo"
                      Style="{StaticResource DarkComboBox}"
                      Width="160"
                      HorizontalAlignment="Left">
                <ComboBoxItem Content="Auto-detect" Tag="auto" IsSelected="True"/>
                <ComboBoxItem Content="1.8.9" Tag="1.8.9"/>
                <ComboBoxItem Content="1.21" Tag="1.21"/>
                <ComboBoxItem Content="26.1" Tag="26.1"/>
                <ComboBoxItem Content="26.2" Tag="26.2"/>
            </ComboBox>
        </DockPanel>

        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,14,0,0">
            <Button Content="Cancel"
                    Style="{StaticResource DarkButton}"
                    Width="90"
                    Margin="0,0,8,0"
                    Padding="12,6"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
            <Button Content="Inject into selected client"
                    Style="{StaticResource DarkButton}"
                    Width="160"
                    Padding="12,6"
                    IsDefault="True"
                    Click="InjectButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
